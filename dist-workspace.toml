# Note: CLI binaries are now in a separate repository: crane-valley/kylix-cli
# This file is kept for reference but cargo-dist is no longer used for this repo.
# Release workflow publishes crates to crates.io only (see .github/workflows/publish.yml).

[workspace]
members = []

[dist]
cargo-dist-version = "0.30.3"
ci = "github"
pr-run-mode = "skip"
